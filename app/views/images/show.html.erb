<%= image_tag @image.url %>
<p><%= @image.title %></p>
<p><%= @image.author %></p>

<%= link_to "Back to Images", images_url %> <%= link_to "Edit Image", edit_image_path(@image) %>
<%= link_to "Delete Image", @picture, method: :delete, data: {confirm: "Are you sure you want to delete this image?"} %>